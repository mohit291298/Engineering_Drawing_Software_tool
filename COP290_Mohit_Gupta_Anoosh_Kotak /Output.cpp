#include <bits/stdc++.h>
#include <main_computation.cpp>
#include <Canvas_3D.cpp>
#include <Canvas_2D.cpp>

using namespace std;
/** @file */

/**
* The output graphs are converted into a list and these lists are printed in an output file.
*/

class output{
public:

	/** A contructor
	* The graphs are converted into a list.
	* Firstly, the vertices having edge between them are identified.
	* Then they are converted into a tuple and added to the list.
	* This list is then stored into a file and the file is given as output to the user.
	*/
	
	output();
		
};